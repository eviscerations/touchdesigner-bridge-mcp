# Security Model — TouchDesigner Bridge MCP

TouchDesigner Bridge MCP は**データ専用**の制御サーフェスです。AIアシスタントは固定された型付きのツールサーフェスを通じて TouchDesigner のオペレーターネットワークを構築・調整し、マシンの前にいる人間がクックとレンダリングを実行します。本ドキュメントでは、そのセキュリティモデル、防御対象の脅威モデル、正直な限界、そして脆弱性の報告方法について説明します。

## 1. 中核となる保証 — そしてその正直な境界

**目標：** アシスタントはオペレーターネットワークを構築できるが、ホスト上で任意コードを実行することはできない。

カタログには `execute_python` のようなツールも、`run`、`eval`、`shell`、`node_op` も存在しません。すべての機能は名前付きでスキーマ検証されたツールです。これは2つのビルド時カナリアと1つの実行時カナリアによって強制されます：

- `assert_no_rce_endpoints()`（エグゼキューター）は、登録済みハンドラーのいずれかが RCE的な名前（`exec`、`eval`、`wrangle`、`hscript`、`run_code`、`os_system`、…）を持つ場合、アームを拒否します。
- `catalog_never_exposes_rce_tools`（ゲートウェイの `cargo test`）は、出荷されるツールサーフェスが汎用的なコード駆動ツールやコードを運ぶオペレーターファミリー（`script` / `execute` / `cplusplus`）を含む場合、ビルドを失敗させます。
- `check_optype_allowed()`（エグゼキューター）は、型名に `script` / `execute` / `cplusplus` のマーカーを持つオペレーターの作成を実行時に拒否し、さらに自身の*データ*をコードとして評価するオペレーターに対する完全一致の拒否リスト（現在は `evaluateDAT`）を適用します。

**正直な境界（これは要となる残存リスクであり、率直に述べます）。** TouchDesigner では、*誤った*パラメーターに設定された値が、TD自身によってホスト Python として評価されうる場合があります。型付きのゲートウェイサーフェスは*許可リスト*ですが、それはエグゼキューター上の汎用的でオープンキーな `set_par` へと落とし込まれ、エグゼキューターのループバックポートはトークンを持つ任意のローカルプロセスから到達可能です。したがって真の境界は、ゲートウェイではなく、TouchDesigner の約17,000のパラメーターサーフェスにわたる**エグゼキューターのパラメーターガードの完全性**（`check_par_allowed` + `check_optype_allowed`）です。これはクローズドソースのサードパーティサーフェスに対する拒否リストであり、完全であると*証明*することはできず、TouchDesigner のバージョン間で脆弱です。独立したレッドチームレビューでは**動作する RCE バイパスは発見されませんでした**が、これは閉じられた境界ではなく、依然として残存リスクです。

パラメーターガードは、順に、いくつかの層で構成されています：

1. **汎用的なコードポインター拒否** — TD がそのテキストをホストコードとして実行する DAT を参照するパラメーター*名*（`callbacks`、`dragscript`、`dropscript`、`datexpr`、…）は、**すべての**オペレーターで拒否されます。これは最大の単一 RCE クラスを構造的に閉じます。
2. **レビュー済みインラインコードシンク拒否** — 設定可能な文字列*値*を TD がコードとして評価する、監査済みの完全一致 `(optype, param)` ペアのリスト（例：`groupSOP.filter`、`tableDAT.cellexpr`、`replicatorCOMP.tscript`）に加え、それらのファミリーのすべての Sequence ブロックのインデックスを網羅する正規表現による一般化。
3. **フェイルクローズドな許可リスト** — ドライバーが供給する `(optype, param)` は、カタログ化されたオペレーターの*既知の*パラメーターである（`reference/catalog.json` に存在する）、その正当な Sequence ブロックのインデックスである、またはライブのカスタムパラメーターである場合にのみ受け付けられます。未知のもの、または出荷されたプローブより新しいものは、素通しされるのではなく**クローズド**に失敗します。

これらのガードは、エグゼキューターの実行時チェックとゲートウェイのビルド時フェンスの間でミラーリングされ、テスト（ゲートウェイフェンス `code_named_params_are_the_known_reviewed_set` に対する `test_code_sink_guard.py`）によって相互チェックされているため、2つの成果物が黙って乖離することはありません。レビュー済みシンクのリストは、監査可能な唯一の場所としてエグゼキューターのソース（`server.py`）に存在します。

## 2. トランスポートと認証

- **ループバックのみ。** TouchDesigner 内の Web Server DAT は、ポート `9980` で `127.0.0.1` にバインドします。マシン外でリッスンするものはなく、ファイアウォールルールは不要です。
- **自動発行されるセッショントークン。** アームすると 128ビットの CSPRNG トークン（`secrets.token_hex(16)`）が発行され、`~/.touchdesigner-bridge-mcp/arm.json` に書き込まれます。ゲートウェイがそれを読み取り、`X-TDMCP-Token` として提示し、エグゼキューターは `secrets.compare_digest` で比較します。ユーザーがトークンを入力したり目にしたりすることは決してありません。これにより、ループバック CSRF / DNS リバインディングのクラス（悪意あるローカル Web ページが `127.0.0.1:9980` に POST するもの）を、実質ゼロの UX コストで無効化します。
- **クロスオリジンの拒否。** すべてのリクエストは — 認証不要の `health` / `validate_*` エンドポイントを含め — 非ループバックの `Origin` または `Host` ヘッダーを持つ場合に拒否され、認証免除のエンドポイントでも DNS リバインドの経路を閉じます。
- **トレースバックの非漏洩。** ハンドラーのエラーは短いメッセージを返します。Python のトレースバックはローカルの Textport にログ出力され、ワイヤー越しに返されることは決してありません（ホスト偵察に対するハードニング）。
- **ボディの上限。** エグゼキューターは 1 MB のリクエストボディ上限を強制し、ゲートウェイは 8 MB を超える受信 JSON-RPC フレームを破棄します（メモリ DoS ガード）。
- **arm.json の ACL。** アーム時に、設定ディレクトリと `arm.json` は `icacls` によってベストエフォートで現在のユーザーに制限され、平文のトークンが誰でも読める状態にならないようにします。

## 3. ファイルシステムの封じ込め

- **単一の作業ディレクトリ。** すべてのファイル操作は、設定された1つの作業ディレクトリ以下に `realpath` で封じ込められます（エグゼキューターの `confined_path`、ゲートウェイの `confine_path`）。両方の層が**同じ**信頼できる情報源 — `arm.json` の `working_dir`、GUI の「Working dir」の Apply で設定される — を読み取り、呼び出しごとに新しく解決されるため、再起動もハードコードされたパスもありません。
- **設定ディレクトリは対象外。** `~/.touchdesigner-bridge-mcp/`（トークン + 同意フラグ）は封じ込めから除外されているため、ファイルを書き込むツールがそこに書き込むことでトークンを読み取ったり同意フラグを反転させたりすることはできません。
- **信頼のルートは対象外。** エグゼキューターパッケージ（`td_executor/*.py`、`INTEGRITY.json`）と `arm.py` ブートストラップは、作業ディレクトリの**内部であっても**拒否されるため、レンダリングや CSV 書き込みが信頼されたファイルに着地し、次回のアームでブリッジを破壊することはできません。
- **拡張子のホワイトリスト。** `save_top` / `capture_ui` は画像のみを書き込み、`write_csv` は `.csv` / `.dat` / `.txt` のみを書き込みます。リファレンスカタログ（`.json`）はそのいずれでもないため、パラメーターガードが依存する許可リストをどのツールも破壊できません。

## 4. インテグリティのピン留め — そしてその正直な上限

`td_executor/INTEGRITY.json` は、すべてのエグゼキューター `.py` ファイルの SHA-256 をピン留めします。信頼を確立する2つの瞬間 — アーム（`arm.py`）と `dev_reload` — はどちらも**インポート前に検証**し、いかなる不一致でも、あるいはピン留めされていないハンドラーモジュールがディスク上に現れた場合でも、クローズドに失敗します。アーム時のチェックは、`td_executor` がインポートされる*前に*ファイルをハッシュする、自己完結型の標準ライブラリブートストラップであるため、改ざんされた `server.py` はそのモジュール本体が実行される前に検出されます。

**正直な上限 — これを過大に述べてはいけません。** INTEGRITY.json は、すでにインストールディレクトリに書き込める攻撃者に対する**改ざんの証跡 / 多層防御であって、境界ではありません**。そのような攻撃者は、ファイルとマニフェストを一緒に書き換える（あるいは、大きくログ出力される `TDMCP_INTEGRITY=0` の開発用バイパスを設定する）ことができます。インポート前検証はハードルを上げます — 改ざんされた検証器はもはや単に成功を返すことはできません — が、**真の信頼のルートはインストールディレクトリに対する OS のファイルパーミッション**です。インテグリティのピン留めは、それらのパーミッションを補完するものであって、決して代替するものではありません。

## 5. 2つの検証済みコードレーン（デフォルトで無効、同意ゲート付き）

いかなるコードも受け入れる唯一の経路は、2つの狭いレーンであり、それぞれ**デフォルトで無効**で、呼び出しごとに新しく読み取られる `arm.json` のフラグでゲートされます。両者ともいかなる書き込みの**前にも**検証し、エグゼキューターが権威を持ち（ゲートウェイは安価な構造的な事前チェックのみを行い、Python 検証器に委ねます）、単一の書き込み経路を持ち、作業ディレクトリの監査ログに追記します。

| レーン | ツール | 同意フラグ | サンドボックス | 検証が見逃した場合の最悪ケース |
|------|-------|--------------|---------|---------------------------------|
| `glsl_v1` | `set_glsl`, `validate_glsl` | `allow_glsl` | GPU | 回復可能なドライバータイムアウト（TDR） |
| `expr_v1` | `set_expr`, `validate_expr` | `allow_expr` | なし（ホスト） | ホストコード — EXPERIMENTAL として出荷 |

- **`set_glsl`** は GLSL フラグメントシェーダーを検証し（ループ / テクスチャフェッチ / サイズの上限とプリプロセッサの許可リスト）、その後、対象の `glslTOP` の下に自身が作成し所有する Text DAT にのみ書き込み、そのオペレーターの `pixeldat` にのみ配線します。ビルドの解像度 / パスは低く（1280×720、パス数 ≤4）クランプされているため、シェーダー自体が TDR のベクトルになることはできません。生のドライバー経路（`set_par`）で `pixeldat` を渡すことは拒否されます — それは `set_glsl` を通さなければなりません。
- **`set_expr`** は、AST のポジティブ許可リストサンドボックスで TouchDesigner のパラメーター式を検証し、MCP 全体でパラメーターの `.expr` を書き込む唯一の場所です。ここでの検証器のギャップは、回復可能な GPU リセットではなくホストコードとなるため、EXPERIMENTAL として出荷されます。その正直な残存リスク（許可リスト化された属性のライブ API 到達可能性）はコード内で開示されており、それがサインオフされるまでレーンは無効のままです。

**AI は自身のレーンを有効化できません。** 同意フラグは対象外の設定ディレクトリに存在し、`/mcp_bridge` コンポーネント（およびフラグをミラーする GUI トグル）は、すべての変更を伴うツールにおいて `assert_writable` によって拒否されます — したがって、GUI の前にいる人間、またはアームブートストラップだけが同意を反転できます。素の再アームは、設定されていた同意をそのまま保持します（レーンを黙って無効化したり有効化したりすることは決してありません）。

## 6. 可用性：強制される規模の上限（F-DOS-1）

リソースガバナーは**助言優先**です — 重いリクエストは規模フラグを提示し、監視しているオペレーターがスケールダウンできるようにしますが、ガバナーは日常的な作業をブロックしません（そしてテレメトリの失敗が作業をブロックすることは決してありません）。TouchDesigner の Python API はカード全体の VRAM の数値を公開しないため、この助言はライブの VRAM ではなく、システム RAM とリクエストごとの規模で分類します。

助言に加えて、**強制される上限**は、既知のパラメーターに対して真に破滅的でドライバーを停止させる規模をハード拒否します（`governor.py`）：次元あたりの解像度 > 16384 px、インスタンス / パーティクル数 > 5,000,000、レンダーパス > 256。これは、汎用的な `set_par {resolutionw: 100000}` が VRAM を枯渇させてディスプレイドライバーをハングさせるのを止めます。これは `arm.json` の人間ゲート付き `allow_highres` フラグによってのみ上書き可能であるため、この機能はガードされているのであって、恒久的に切除されているのではありません。正当な 4K/8K の出力は余裕をもって通過します。

## 7. 脅威モデル

防御対象：

- **完全に敵対的な / プロンプトインジェクションされた AI** がゲートウェイを駆動する（またはトークンを持ってエグゼキューターに直接アクセスする）場合：型付きのデータサーフェス + パラメーターガードに封じ込められ、ホストコードを実行したり、作業ディレクトリから脱出したり、トークンを読み取ったり、自身の同意を反転させたりすることはできません。
- **ネットワークローカルの攻撃者**（ブラウザページ、別のローカルプロセス）：ループバックバインド + クロスオリジン拒否 + トークン認証が CSRF / DNS リバインドの経路を閉じます。トークンファイルはユーザーに ACL 制限されています。
- **悪意ある入力の攻撃者**：ボディの上限、JSON 検証、コードレーンの検証器、そして強制される規模の上限が、リソースとパースの濫用を制約します。

明示的に**対象外**：

- すでにインストールディレクトリまたは TouchDesigner を実行しているユーザーアカウントへの**書き込みアクセス**を持つ攻撃者。ここでインテグリティのピン留めは改ざんの証跡であって境界ではありません。OS のファイルパーミッションが信頼のルートです。
- すでに `arm.json` のトークンを保持している任意のローカルプロセス — それはゲートウェイとまったく同じようにエグゼキューターに到達できます。境界は、クライアントの身元ではなく、ループバック + パラメーターガードです。
- TouchDesigner、OS、GPU ドライバー、または AI クライアント自体の脆弱性。
- §1 で述べた、パラメーター拒否リストの残存する完全性リスク。

## 8. 脆弱性の報告

**セキュリティ上の脆弱性には公開 Issue を使用しないでください。**

GitHub のプライベート脆弱性報告を使用してください：
1. [Security タブ](https://github.com/eviscerations/touchdesigner-bridge-mcp/security)にアクセスします
2. **Report a vulnerability** をクリックして、プライベートセキュリティアドバイザリを開きます
3. 再現に十分な詳細を添えて問題を説明します — 影響を受けるコンポーネント、再現手順または概念実証（PoC）、そして観測された影響
    
TouchDesigner がコードとして評価するにもかかわらずガードが拒否**しない**、確認済みのパラメーター（`check_par_allowed` / `check_optype_allowed` のバイパス）は、最も価値の高い報告です — §1 を参照してください。

7日以内に返答します。脆弱性が確認された場合、可能な限り速やかに修正がリリースされ、ご希望であればリリースノートにクレジットを記載します。
## 9. バージョン

本ドキュメントはバージョン **0.1.0** を説明しています。セキュリティに関連する変更は [CHANGELOG.md](CHANGELOG.md) で追跡されます。
